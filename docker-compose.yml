version: '3.8'

services:
  backend-ai:
    build: .
    container_name: durian-ai-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 2G