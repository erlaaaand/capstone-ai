services:
  backend-ai:
    build: .
    container_name: durian-ai-backend
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./hf_cache:/home/appuser/.cache/huggingface